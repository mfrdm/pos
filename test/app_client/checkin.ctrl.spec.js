describe ('Controller: CheckinCtrl', function (){
	// beforeEach (module('posApp'));
	// var ctrl;
	// beforeEach (inject (function ($controller){
	// 	ctrl = $controller ('CheckinCtrl');
	// }));

	it('should success pull check-in items on load', function (){
		// expect (ctrl.status.getCheckinList).tobeDefined ();
		expect (2 + 2).toEqual (4);
	});

});